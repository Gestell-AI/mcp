{
  "$ref": "#/definitions/collection.list",
  "definitions": {
    "collection.list": {
      "type": "object",
      "properties": {
        "search": {
          "type": "string",
          "description": "Optional filter string to match name, description, or tags. Example: \"finance Q2\"."
        },
        "take": {
          "type": "integer",
          "description": "Optional maximum number of collections to return. Example: 5."
        },
        "skip": {
          "type": "integer",
          "description": "Optional number of collections to skip (pagination offset). Example: 10."
        },
        "extended": {
          "type": "boolean",
          "description": "Optional flag to include extended details (documents in collection etc.)."
        }
      },
      "additionalProperties": false
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}